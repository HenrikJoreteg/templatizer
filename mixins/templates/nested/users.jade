mixin user_li(user)
    li(data-user-id=user.id)
        span Before the li
        mixin user_a(user)

mixin user_a(user)
    a(href=user.url) Nested #{user.name}

ul.users
    each user in users
        mixin user_li(user)